BITSID : 2019H1030017P
Name : Lucky Agrawal
Email : H20190017@pilani.bits-pilani.ac.in

tee.c is can be compiled easily using make file

cmd :: make

This will generate tee.out as an output file.

Now to run the funcionality of tee command execute the above file as::

cmd  :: ./tee.out

o/p  :: Enter your command > 
 
cmd  :: Enter your command > ps -al ~ outputfile1.txt outputfile2.txt ...

here '~' is used as an tee command.

This will output to console and also store them in file specified.

